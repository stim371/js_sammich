<h1>Listing sandwich_orders</h1>

<table>
  <tr>
    <th>Ordering person</th>
    <th>Sandwich type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sandwich_orders.each do |sandwich_order| %>
  <tr>
    <td><%= sandwich_order.ordering_person %></td>
    <td><%= sandwich_order.sandwich_type %></td>
    <td><%= link_to 'Show', sandwich_order %></td>
    <td><%= link_to 'Edit', edit_sandwich_order_path(sandwich_order) %></td>
    <td><%= link_to 'Destroy', sandwich_order, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sandwich order', new_sandwich_order_path %>
